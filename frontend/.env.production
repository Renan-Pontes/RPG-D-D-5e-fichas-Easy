# Build de produção (Vercel)
# Substitua VITE_API_URL pelo URL do seu backend PythonAnywhere quando subir.
# Enquanto não setar, o frontend detecta backend offline e ativa modo
# standalone com banner explicativo.
VITE_API_URL=https://api-not-set.example
