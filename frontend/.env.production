# Build de produção (Vercel)
# Backend de produção rodando no PythonAnywhere (free tier, polling 2.5s).
# Se este URL ficar offline, o frontend detecta e ativa modo standalone com
# banner explicativo (ver AuthContext.backendAvailable).
VITE_API_URL=https://mestresdd5e.pythonanywhere.com
